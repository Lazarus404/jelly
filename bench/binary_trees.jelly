let pow2: I32 -> I32 = fn(k) {
  let r: I32 = 1;
  let i: I32 = 0;
  while (i < k) {
    r = r + r;
    i = i + 1;
  }
  return r;
};

// NOTE: This port is currently work-in-progress; see `bench/wip/neko_binary_trees.jelly`.
"unsupported"

