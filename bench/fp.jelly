let x: F64 = 1.0;
let y: F64 = 1.0;
let i: I32 = 1000000;

while (i > 0) {
  x = x * 1.000000001;
  y = y / 1.000000001;
  i = i - 1;
}

let p: F64 = x * y;
let d: F64 = if (p > 1.0) { p - 1.0 } else { 1.0 - p };
System.assert(d < 0.000001);
"ok"

