let init: (Object, I32, Bytes) -> Object = fn(self, n, s) {
  self.n = n;
  self.s = s;
  return self;
};

let P = { init: init };
let o = new P(7, "hi");

let ok = (o.n == 7) && (Bytes.len(o.s) == 2);
let r = if (ok) { "ok" } else { "bad" };
System.assert(r == "ok");
r
