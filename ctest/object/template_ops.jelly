let add<T>: (Object, T, T) -> T = fn(self, a, b) {
  return a + b;
};

let o = {};
let x: I32 = add<I32>(o, 2, 3);
let y: Bytes = add<Bytes>(o, "a", "b");

let r = if (x == 5 && y == "ab") { "ok" } else { "bad" };
System.assert(r == "ok");
r
