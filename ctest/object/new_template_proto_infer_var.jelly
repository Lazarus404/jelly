prototype Box<T> {
  a: "ok";
};

let x: I32 = 7;
let o = new Box(x);

let ok =
  (Bytes.len(o.a) == 2) &&
  (Bytes.get_u8(o.a, 0) == 111) &&
  (Bytes.get_u8(o.a, 1) == 107);
let r = if (ok) { "ok" } else { "bad" };
System.assert(r == "ok");
r
