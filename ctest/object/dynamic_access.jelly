let o = { };
let k = Atom.intern("a");
let _ = Object.set(o, k, "ok");
let v: Bytes = Object.get<Bytes>(o, k);
let ok =
  (Bytes.len(v) == 2) &&
  (Bytes.get_u8(v, 0) == 111) &&
  (Bytes.get_u8(v, 1) == 107);
if (ok) { "ok" } else { "bad" }

