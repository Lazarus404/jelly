let init: (Object, I32) -> Object = fn(self, n) {
  self.n = n;
  return self;
};

let get: (Object) -> I32 = fn(self) {
  return self.n;
};

prototype P {
  init: init;
  get: get;
};

let o = new P(7);
let r: I32 = o.get();
if (r == 7) { "ok" } else { "bad" }

