prototype Box<T> {
  a: "ok";
};

let x: I32 = 7;
let o = new Box(x);

let ok =
  Bytes.eq(o.a, "ok") &&
  Bytes.eq(o.__template__, "Box") &&
  Bytes.eq(o.__type_args__, "I32");

let r = if (ok) { "ok" } else { "bad" };
System.assert(r == "ok");
r
