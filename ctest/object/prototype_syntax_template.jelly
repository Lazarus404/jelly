let init<T>: (Object, T) -> Object = fn(self, v) {
  self.v = v;
  return self;
};

prototype Box<T> {
  init: init<T>;
};

let b = new Box(7);
let r = if (b.v == 7) { "ok" } else { "bad" };
System.assert(r == "ok");
r
