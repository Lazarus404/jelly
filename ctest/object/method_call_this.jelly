let add: (Object, I32) -> I32 = fn(self, y) {
  let x: I32 = self.x;
  return x + y;
};
let o = { x: 4, add: add };
let h: I32 = o.add(3);
let r = if (h == 7) { "ok" } else { "bad" };
System.assert(r == "ok");
r
