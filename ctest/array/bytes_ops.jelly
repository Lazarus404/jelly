let b = Bytes.new(1);
let _b2 = Bytes.set_u8(b, 0, 65);
let okb = (Bytes.get_u8(b, 0) == 65);

let a = Array.new<I32>(3);
let _a0 = Array.set(a, 0, 7);
let _a1 = Array.set(a, 1, 8);
let oka = (Array.get(a, 1) == 8) && (Array.len(a) == 3);

if (okb && oka) { "ok" } else { "bad" }

