let ack = fn(x, y) {
  return if (x == 0) {
    y + 1
  } else {
    if (y == 0) {
      ack(x - 1, 1)
    } else {
      ack(x - 1, ack(x, y - 1))
    }
  };
};

let r = ack(3, 4);
System.assert(r == 125);
"ok"

