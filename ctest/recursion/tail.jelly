let sum: (I32, I32) -> I32 = fn(n, acc) {
  return if (n == 0) { acc } else { sum(n - 1, acc + n) };
};

let h: I32 = sum(10000, 0);
let r = if (h == 50005000) { "ok" } else { "bad" };
System.assert(r == "ok");
r
